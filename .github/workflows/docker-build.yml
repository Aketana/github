name: Docker Build

on:
  push:

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show Files
        run: ls -la

      - name: Build Docker Image
        run: docker build -t demo-app .